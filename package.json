{
  "name": "logikids",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "docker compose up frontend-dev backend-dev",
    "prod": "docker compose up frontend-prod backend-prod",
    "build": "docker compose build",
    "check-translations": "bun scripts/check-translations.ts",
    "check-concept": "docker compose exec backend-dev bun run check:concept",
    "check-subject": "docker compose exec backend-dev bun run check:subject"
  },
  "comments": {
    "ports": "Frontend dev: 5153, Frontend prod: 5154, Backend dev: 5175, Backend prod: 5176"
  },
  "devDependencies": {
    "@rollup/rollup-darwin-x64": "^4.53.2"
  }
}