{
  "name": "logikids",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "docker compose up frontend-dev backend-dev",
    "prod": "docker compose up frontend-prod backend-prod",
    "build": "docker compose build",
    "check:concepts": "docker compose exec backend-dev bun run check:concepts",
    "check:prompts": "docker compose exec backend-dev bun run check:prompts",
    "check:translations": "docker compose exec backend-dev bun run check:translations",
    "generate:prompt": "docker compose exec backend-dev bun run generate:prompt",
    "generate:task": "docker compose exec backend-dev bun run generate:task"
  },
  "comments": {
    "ports": "Frontend dev: 5153, Frontend prod: 5154, Backend dev: 5175, Backend prod: 5176"
  },
  "devDependencies": {
    "prettier": "^3.7.3"
  }
}