{
  "name": "logikids",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "docker compose up frontend-dev backend-dev",
    "prod": "docker compose up frontend backend",
    "build": "docker compose build",
    "check:concepts": "docker compose exec scripts bun run check:concepts",
    "check:translations": "docker compose exec scripts bun run check:translations",
    "check:orphaned-translations": "docker compose exec scripts bun run check:orphaned-translations",
    "check:prompts": "docker compose exec backend-dev bun run check:prompts",
    "generate:prompt": "docker compose exec backend-dev bun run generate:prompt",
    "generate:task": "docker compose exec backend-dev bun run generate:task"
  },
  "comments": {
    "ports": "Frontend dev: 5153, Frontend: 5154, Backend dev: 5175, Backend: 5176"
  },
  "devDependencies": {
    "prettier": "^3.7.3"
  }
}
