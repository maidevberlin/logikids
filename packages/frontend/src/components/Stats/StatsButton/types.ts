export interface StatsButtonProps {
  /** Optional click handler. If not provided, will navigate to /stats */
  onClick?: () => void
  /** Optional class name for additional styling */
  className?: string
} 