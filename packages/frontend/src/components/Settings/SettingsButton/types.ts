export interface SettingsButtonProps {
  /** Optional click handler. If not provided, will navigate to /account */
  onClick?: () => void
  /** Optional class name for additional styling */
  className?: string
} 